# tmux-conf
#   - minimalist and fantasmagoric looking tmux configuration
#   - ...
# rodrigo-pariente
#   - github: github.com/rodrigo-pariente
#   - email : rodrigo_pariente@proton.me
# credits
#   - inspired by ham vocke's `Make tmux Pretty and Usable` article:
#     hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/

# reload config file (change file location to your the tmux.conf you want to use)
bind r source-file ~/.config/tmux/tmux.conf

# switch panes using Alt-arrow without prefix
bind -n M-h select-pane -L
bind -n M-l select-pane -R
bind -n M-k select-pane -U
bind -n M-j select-pane -D

# vi keybinding
set -g mode-keys vi
setw -g mode-keys vi
# set-window-option mode-keys vi

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

# DESIGN TWEAKS

# status bar
set -g status-position bottom
set -g status-justify left
set -g status-style 'bg=colour233'
set -g status-right ''
set -g status-left ''

setw -g window-status-current-format ' [#I:#F] '
setw -g window-status-format '#I:#W#F'
setw -g window-status-current-style 'fg=colour240'
setw -g window-status-style 'fg=colour237'

# don't do anything when a 'bell' rings
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

# clock mode
setw -g clock-mode-colour magenta

# panes
set -g pane-active-border-style 'fg=magenta'
set -g pane-border-style 'fg=green'
